#![no_implicit_prelude]

type Box('a) = Boxed('a)

trait Functor 'f {
    fn map(fa: 'f('a), f: fn('a) -> 'b) -> 'f('b)
}

impl Functor for Box('x) {
    fn map(fa, f) {
        match fa {
            Boxed(value) -> Boxed(f(value)),
        }
    }
}
