fn bump(mut r: #{ x: int }) {
  r.x = 2;
}

fn use_ops(ops: #{ bump: fn(mut #{ x: int }) -> Unit }, mut r: #{ x: int }) {
  ops.bump(r);
}

let ops = #{ bump: bump };
let mut r = #{ x: 1 };
use_ops(ops, r);
print(r.x)
