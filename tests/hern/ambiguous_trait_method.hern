trait Runner 'a {
    fn run(x: 'a) -> float
}

trait Executor 'a {
    fn run(x: 'a) -> float
}

impl Runner for float {
    fn run(x) { x }
}

impl Executor for float {
    fn run(x) { x + 1 }
}

// ambiguous: both Runner and Executor have `run`
print(run(1));
