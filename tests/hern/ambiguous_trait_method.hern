trait Runner 'a {
    fn run(x: 'a) -> float
}

trait Executor 'a {
    fn run(x: 'a) -> float
}

impl Runner for float {
    fn run(x) { x }
}

impl Executor for float {
    fn run(x) { x + 1.0 }
}

// ambiguous: both Runner and Executor provide `.run()`
let x = 1.0;
print(x.run());
