trait Double 'a {
    fn double(x: 'a) -> 'a
}

impl Double for f64 {
    fn double(x) { x + x }
}

// bare call — dispatches via Double without explicit trait prefix
print(double(21));
print(double(0));
