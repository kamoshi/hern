trait Double 'a {
    fn double(x: 'a) -> 'a
}

impl Double for float {
    fn double(x) { x + x }
}

// bare call — dispatches via Double without explicit trait prefix
print(double(21.0));
print(double(0.0));
