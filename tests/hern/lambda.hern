let add = fn(x, y) -> x + y;
print(add(3.0, 4.0));

let mul = fn(x, y) { x + x + x };
print(mul(5.0, 0.0));

fn apply(f, x) { f(x) }
print(apply(fn(n) -> n + 10.0, 5.0));

let inc: fn(f64) -> f64 = fn(x: f64) -> x + 1.0;
print(inc(41.0));
