type Counter = Counter(float)

impl Counter {
  fn make(value: float) -> Self {
    Counter(value)
  }

  fn value(self) -> float {
    match self {
      Counter(value) -> value,
    }
  }
}

let counter = Counter::make(41.0);

print(counter.value());
