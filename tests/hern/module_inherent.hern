type Boxed = MkBoxed(float) | Empty

impl Boxed {
  fn value(self) -> float {
    match self {
      MkBoxed(v) -> v,
      Empty -> 0.0,
    }
  }
}

impl Boxed {
  fn fallback(self) {
    match self {
      MkBoxed(v) -> v,
      Empty -> 99.0,
    }
  }
}

MkBoxed(11.0)
