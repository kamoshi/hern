type MathModule = #{
  floor: fn(float) -> float,
  abs: fn(float) -> float,
  max: fn(float, float) -> float
}

extern math: MathModule = "math";

fn apply_floor(m, x) {
  m.floor(x)
}

print(apply_floor(math, 3.7))
print(math.abs(0.0 - 42.0))
print(math.max(3.0, 7.0))
