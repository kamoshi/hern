fn apply_map(mapper, fa, f) {
  mapper(fa, f)
}

let map = Functor::map;

let a = apply_map(map, Some(2), fn(x) { x + 3 });
let b = apply_map(map, [2, 3], fn(x) { x + 4 });

print(a.unwrap_or(0));
print(b[0]);
print(b[1]);
