trait Label 'a {
    fn label(x: 'a) -> string
}

impl Label for ['a] {
    fn label(xs) { "generic" }
}

impl Label for [float] {
    fn label(xs) { "numbers" }
}

print(label([1.0]));
print(label(["a"]));
